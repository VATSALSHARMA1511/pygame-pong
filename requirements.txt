# requirements.txt
# Python dependencies for the Pong game.
#
# Install with:
#     pip install -r requirements.txt
#
# Python 3.10+ is recommended (uses the X | Y union type hint syntax).

pygame==2.5.2
